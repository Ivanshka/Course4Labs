1. В чем отличие явного и неявного объекта Intent?
	Явная  -
		Intent intent = new Intent(HelloWorld.this, AboutActivity.class);
		startActivity(intent);
	Неявная - 
		Выбирается представление по умолчанию из категории, Расскажу на примере бритвы только спроси

2. Как происходит разрешение объектов Intent?
	Через класс или через фильтр (Смотреть в манифесте)

3. Как определить действие для запуска Activity?
	startActivity()

4. Что определяет категория в Intent? Приведите примеры
	Категория абстрактно определяет что должна делать activity и в случае обращение к activity данной категории будет вызвана доступная activity из этой категории

5. Перечислите несколько общих Intent?
	ACTIVE_VIEW
	ACTIVE_SEND
	ACTIVE_DIAL

6. Для чего нужны флаги классе Intent? Приведите примеры
	 Это метаинформация, например для работы со стеком активностей

7. Поясните принцип работы фильтров Intent?
	Поиск вызываемой активности осуществляется не через имя класса, а через строку указанную в фильтре	

8. Охарактеризуйте Internal Storage.
	Внутренее хранилище ведра	

9. Расскажите как записать, прочитать файл в/из Internal Storage.
	Смотри манифест на uses-permissions и скажи, как при работе с обычной файловой системой на Java

10. Охарактеризуйте External Storage.
	SD-карта	

11. Расскажите как записать, считать файл SharedPreferences. Для чего
используют такие файлы?
getPreferences() — внутри активности, чтобы обратиться к определённому для активности предпочтению;
getSharedPreferences() — внутри активности, чтобы обратиться к предпочтению на уровне приложения;
getDefaultSharedPreferences() — из объекта PreferencesManager, чтобы получить общедоступную настройку, предоставляемую Android.

Все эти методы возвращают экземпляр класса SharedPreferences, из которого можно получить соответствующую настройку с помощью ряда методов:

getBoolean(String key, boolean defValue)
getFloat(String key, float defValue)
getInt(String key, int defValue)
getLong(String key, long defValue)
getString(String key, String defValue)